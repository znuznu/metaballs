/** Metaballs quantity. */
export const QUANTITY = 24;

/** Minimum metaballs size (radius). */
export const MAX_RADIUS = 60;

/** Minimum metaballs size (radius). */
export const MIN_RADIUS = 10;

/** Metaballs color. */
export const META_COLOR = '#007acc';

/** Grid's square color. */
export const SQUARE_COLOR = '#007acc';

/** Size of the grid's square. */
export const SQUARE_SIZE = 4;